export interface Device {
    id: string
    name: string
    uuid: string
    os: string
    model: string
    platform: string
    pushEnabled: boolean
    registrationToken: string
    createdAt: string
    updatedAt: string
}